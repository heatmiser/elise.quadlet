---
galaxy_info:
  author: heatmiser
  description: >-
    Start Podman Quadlet services in health-gated start_order tiers.
    Pull mode only — embedded mode startup is owned by systemd after first boot.
  license: Apache-2.0
  min_ansible_version: "2.16"
  platforms:
    - name: EL
      versions:
        - "9"
  galaxy_tags:
    - podman
    - quadlet
    - containers
    - systemd

dependencies: []
...
